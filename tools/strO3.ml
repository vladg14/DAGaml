let int = (string_of_int, int_of_string)
let bool = StrUtil.(string_of_bool, bool_of_string)
let bitv_hexa = Bitv.L.(to_hexa_string, of_hexa_string)
let bitv_bool = Bitv.L.(to_bool_string, of_bool_string)
